// menuItemOrdered is type array with: amount, what was ordered, special wishes/accommodation

class Order {
  constructor(id, menuItemOrdered) {
    this.id = id;
    this.menuItemOrdered = menuItemOrdered;
  }
}
